<figure contenteditable="false">
    <img src="{{img}}" alt="" />
    {{#if progress }}
        <div class="medium-insert-images-progress">
        <img src="{{img}}" alt="" />
        </div>

    {{/if}}
</figure>
